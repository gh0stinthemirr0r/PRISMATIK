<script lang="ts">
  import { onMount } from 'svelte';
  import { invoke, isTauri } from '@tauri-apps/api/core';
  let integrationActive = $state<Record<string,boolean>>({});
  let query = $state('');
  onMount(async()=>{if(isTauri()){const rows=await invoke<Array<{providerId:string;active:boolean}>>('integration_runtime_status');integrationActive=Object.fromEntries(rows.map(row=>[row.providerId,row.active]));}});
  const venues=[{id:'polymarket',name:'Polymarket',state:'native order-book normalizer',detail:'CLOB pricing, executable depth, event rules and resolution evidence.'},{id:'kalshi',name:'Kalshi',state:'native fixed-point normalizer',detail:'Regulated event contracts, order-book depth and explicit resolution sources.'}];
  const filtered=$derived(venues.filter(v=>`${v.name} ${v.detail}`.toLowerCase().includes(query.toLowerCase())));
</script>
<svelte:head><title>Prediction markets · PRISMATIK</title></svelte:head>
<div class="prediction">
  <header><div><span>PREDICTION FABRIC / SEMANTICALLY SAFE COMPARISON</span><h1>Event truth</h1><p>PRISMATIK compares executable probability only after canonical identity, exact resolution language, fees, depth, timestamps, and semantic equivalence pass.</p></div><a href="/workspace/integrations">CONNECT VENUES →</a></header>
  <section class="flow"><b>VENUE CONTRACTS</b><i></i><b>SEMANTIC MATCH</b><i></i><b>FEE + DEPTH</b><i></i><b>RESOLUTION RISK</b><i></i><b>CROSS-VENUE EDGE</b></section>
  <div class="body">
    <section class="venue-panel"><div class="panel-head"><h2>Venue coverage</h2><input bind:value={query} placeholder="Filter venues" /></div>{#each filtered as venue}<article><div><strong>{venue.name}</strong><span>{venue.state}</span></div><p>{venue.detail}</p><em class:active={integrationActive[venue.id]}>{integrationActive[venue.id]?'ACTIVE':'AWAITING GOVERNED ADAPTER'}</em></article>{/each}</section>
    <section class="opportunity"><div class="panel-head"><h2>Executable comparisons</h2><span>0 CANONICAL MATCHES</span></div><div class="empty"><div class="rings"><i></i><i></i><i></i></div><strong>No venue observations</strong><p>No probability, spread, arbitrage, or liquidity values are synthesized. Activate approved venue adapters to populate canonical contracts.</p></div></section>
  </div>
  <section class="risk"><article><span>IDENTITY GATE</span><b>Title matching prohibited</b><p>Event, market, contract, outcome, and venue-native identifiers remain distinct.</p></article><article><span>SEMANTIC GATE</span><b>Exact rules retained</b><p>Different resolution sources or material language prevent automatic merging.</p></article><article><span>EXECUTION GATE</span><b>Read-only by default</b><p>Positive spread is research until fees, depth, transfer constraints, risk, and account gates pass.</p></article></section>
</div>
<style>
.prediction{height:100%;overflow:auto;padding:34px clamp(22px,3vw,48px) 80px;color:var(--p-text)}header{display:flex;justify-content:space-between;gap:30px;align-items:end}header span,.panel-head span{font:600 .58rem var(--font-mono);letter-spacing:.13em;color:var(--p-accent)}h1{font-size:clamp(2.8rem,5vw,5.5rem);letter-spacing:-.07em;margin:8px 0}header p{max-width:800px;color:var(--p-dim);line-height:1.55}header a{border:1px solid var(--p-border);padding:11px;color:var(--p-accent);text-decoration:none;font:.62rem var(--font-mono);white-space:nowrap}.flow{display:flex;align-items:center;overflow:auto;margin:28px 0;border-block:1px solid var(--p-border);padding:17px}.flow b{font:.56rem var(--font-mono);white-space:nowrap}.flow i{height:1px;min-width:25px;flex:1;background:linear-gradient(90deg,var(--p-accent),var(--p-border));margin:0 12px}.body{display:grid;grid-template-columns:minmax(300px,.7fr) minmax(400px,1.3fr);gap:12px}.venue-panel,.opportunity,.risk article{border:1px solid var(--p-border);background:var(--p-panel-fill);backdrop-filter:blur(var(--glass-blur))}.panel-head{display:flex;align-items:center;justify-content:space-between;padding:16px;border-bottom:1px solid var(--p-border)}h2{font-size:1rem;margin:0}.panel-head input{width:150px;border:1px solid var(--p-border);background:transparent;color:var(--p-text);padding:7px}.venue-panel article{padding:16px;border-bottom:1px solid var(--p-border)}.venue-panel article>div{display:flex;justify-content:space-between}.venue-panel span,.venue-panel p{color:var(--p-dim);font-size:.7rem}.venue-panel p{line-height:1.5}.venue-panel em{font:normal .55rem var(--font-mono);color:#ffbf69}.venue-panel em.active{color:#67efba}.empty{min-height:310px;display:grid;place-content:center;text-align:center;padding:30px}.empty p{max-width:470px;color:var(--p-dim);line-height:1.55}.rings{position:relative;width:80px;height:80px;margin:0 auto 20px}.rings i{position:absolute;inset:0;border:1px solid var(--p-border);border-radius:50%}.rings i:nth-child(2){inset:14px;border-color:var(--p-accent)}.rings i:nth-child(3){inset:30px;background:var(--p-accent);box-shadow:0 0 20px var(--p-accent)}.risk{display:grid;grid-template-columns:repeat(3,1fr);gap:12px;margin-top:12px}.risk article{padding:18px}.risk span{font:.56rem var(--font-mono);color:var(--p-accent)}.risk b{display:block;margin:12px 0}.risk p{color:var(--p-dim);font-size:.72rem;line-height:1.5}@media(max-width:900px){.body{grid-template-columns:1fr}.risk{grid-template-columns:1fr}}@media(max-width:620px){header{display:block}header a{display:inline-block;margin-top:10px}}
</style>
